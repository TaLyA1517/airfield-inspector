# Install order matters — see README for the correct pip command.
# opencv-python-headless must be present before ultralytics is resolved
# so that the libGL-free cv2 build is used.
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.12
opencv-python-headless==4.10.0.84
ultralytics==8.3.0
Pillow==10.4.0
reportlab==4.2.2
aiofiles==24.1.0
